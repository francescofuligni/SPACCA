package application.Card;

import application.Player.Player;
import application.Player.PlayerList;

public class Taverna extends Card{

	public Taverna(int c) {
		super(c);
	}
	
	@Override
	public void effect(Player p) {
	 /* se gli passassi playerlist come parametro il codice sarebbe il seguente:
	  * 
	  * players.getCurrentPlayer.setLifePoints(players.getCurrentPlayer.getLifePoints() + 5);
	  * 
	  * if(players.getList().size() == 2)
	  * players.ge DA FINIRE
	  *
	  
	  */
	 
	}
}
